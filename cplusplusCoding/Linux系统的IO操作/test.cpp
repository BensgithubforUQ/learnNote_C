#include<iostream>
using namespace std;
//虚拟地址空间，首先执行程序，该程序就有一个对应的虚拟地址空间
//虚拟地址空间分为用户区和内核区
//用户区有栈空间（数据从高地址向低地址存）和堆空间（从低地址向高地址存）
//还有共享库，和受保护的地址，代码段
//data段（已经初始化的全局变量），bss段（未初始化的全局变量）
//环境变量和命令行参数等。
//内核区，内存管理，进程管理，设备驱动管理，VFS虚拟文件系统
//普通用户没有读写操作，需要调用系统调用，来操作。
//进程是系统为程序分配的最小运行单位。

int main(){
    FILE
    return 0; 
}
